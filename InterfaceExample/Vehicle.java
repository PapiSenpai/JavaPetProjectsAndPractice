package InterfaceExample;

// Create Interface so that classes can access it
interface Vehicle { 
      
    // all are the abstract since within interface 
    void changeGear(int a); 
    void speedUp(int a); 
    void applyBrakes(int a); 
} 